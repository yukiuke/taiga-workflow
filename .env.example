# Copy to .env for local tooling (never commit .env).
# Used when running MCP locally against a Taiga instance.

TAIGA_API_URL=https://taiga.example.com
TAIGA_USERNAME=agent-bot
TAIGA_PASSWORD=change-me
TAIGA_SERVER_MODE=workflow
TAIGA_TRANSPORT=stdio
LOG_LEVEL=INFO
